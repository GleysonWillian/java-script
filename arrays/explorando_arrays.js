// Criando uma lista em array de strings:
let cargos = ["Médico", "Admin", "Advogado", "Analista de Sistemas"];

// Selecionando um item dentro da lista de array de string:
// cargos[3];

// Alterando o valor dinamicamente:
// cargos[2] = "Contador";

// Adicionando um item na lista:
// cargos.push("Devops");

// Removendo o último item da lista:
// cargos.pop();

// Removendo um item específico da lista:
cargos.splice(2, 1);

// Visualizando no console:
console.log(cargos);